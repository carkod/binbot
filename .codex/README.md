# Codex Workspace Notes

Before running Python commands, activate the project virtualenv:

```sh
source /Users/carkod/binbot/api/.venv/bin/activate
```

This repository uses the API virtualenv under `api/.venv`.
